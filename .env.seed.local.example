# Copia a .env.seed.local para usar seed local.
# Uso: npm run seed:local (desde la raíz)
# Requiere: PostgreSQL local (localhost:5432), BD comercial_electrica, usuario ce / ce_password.

DATABASE_URL="postgresql://ce:ce_password@localhost:5432/comercial_electrica?schema=public"
PLATFORM_ADMIN_EMAIL=
PLATFORM_ADMIN_PASSWORD=
